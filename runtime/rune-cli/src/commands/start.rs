use anyhow::Result;

pub async fn run(
    _dev: bool,
    _binary: Option<String>,
    _image: Option<String>,
    _tag: Option<String>,
    _http_port: Option<u16>,
    _grpc_port: Option<u16>,
    _foreground: bool,
) -> Result<()> {
    todo!("start command -- future task")
}
